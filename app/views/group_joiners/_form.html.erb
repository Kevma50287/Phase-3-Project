<%= form_with(model: group_joiner) do |form| %>
  <% if group_joiner.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(group_joiner.errors.count, "error") %> prohibited this group_joiner from being saved:</h2>

      <ul>
        <% group_joiner.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :isAdmin?, style: "display: block" %>
    <%= form.check_box :isAdmin? %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.number_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
