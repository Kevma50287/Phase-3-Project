<%= form_with(model: tasks_joiner) do |form| %>
  <% if tasks_joiner.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tasks_joiner.errors.count, "error") %> prohibited this tasks_joiner from being saved:</h2>

      <ul>
        <% tasks_joiner.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.number_field :group_id %>
  </div>

  <div>
    <%= form.label :task_id, style: "display: block" %>
    <%= form.number_field :task_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
